name: strikevision
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip>=24
  - ffmpeg
  - git
  - graphviz
  - pip:
      # Local CPU environment. Azure integrations are installed separately when needed.
      - -r requirements.txt

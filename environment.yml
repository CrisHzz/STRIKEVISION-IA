name: strikevision
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip>=24
  - ffmpeg
  - git
  - graphviz
  - pip:
      # Includes Gradio and Streamlit for the local StrikeVision web interfaces.
      - -r requirements.txt
